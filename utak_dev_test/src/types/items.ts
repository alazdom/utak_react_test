export interface NewItemType {
    id?: string;
    name?: string;
    description?: string;
    price?: string;
}

export interface AddItemType {
    name: string;
    description: string;
    price: string;
}